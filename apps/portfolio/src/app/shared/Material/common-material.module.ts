import { NgModule } from '@angular/core';

/**
 * @deprecated All Angular Material components have been migrated to Spartan UI.
 * This module is kept as an empty shell for backward compatibility and can be safely removed.
 */
@NgModule({})
export class CommonMaterialModule {}
